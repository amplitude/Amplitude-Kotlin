# Unified versions independently of analytics-core/android (see unified/release.config.js).
# Overrides the root VERSION_NAME for this module only; semantic-release replaces it on each release.
VERSION_NAME=1.0.0-SNAPSHOT
